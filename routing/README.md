# SegaMap routing backend

This directory contains the server-side routing foundation for SegaMap.

The target architecture is OpenTripPlanner 2 + OpenStreetMap + an official NLTA-derived GTFS feed. OTP is intentionally kept separate from the current frontend while the multimodal routing engine is validated.
