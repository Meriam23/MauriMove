name: Sync official NLTA bus data
on:
  workflow_dispatch:
  schedule:
    - cron: '17 3 * * 1'
  push:
    paths:
      - 'scripts/fetch_nlta.py'
      - 'scripts/patch_runtime.py'
      - '.github/workflows/nlta-data.yml'
      - 'index.html'
permissions:
  contents: write
jobs:
  sync:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 0
      - uses: actions/setup-python@v5
        with:
          python-version: '3.x'
      - name: Install parser
        run: pip install pypdf
      - name: Download and parse NLTA official PDFs
        run: python scripts/fetch_nlta.py
      - name: Apply runtime fixes
        run: python scripts/patch_runtime.py
      - name: Commit generated data and runtime
        run: |
          git config user.name 'github-actions[bot]'
          git config user.email '41898282+github-actions[bot]@users.noreply.github.com'
          git add data/nlta-routes.json index.html scripts/patch_runtime.py
          if git diff --cached --quiet; then
            echo 'No generated changes.'
            exit 0
          fi
          git commit -m 'Update NLTA data and routing runtime'
          git pull --rebase origin main
          git push origin HEAD:main
